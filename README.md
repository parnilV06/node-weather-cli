# node-weather-cli

A simple Node.js command-line interface (CLI) application that fetches and displays weather data for a specified location using the free Open-Meteo API (no API key required).
Made using Github Co-pilot